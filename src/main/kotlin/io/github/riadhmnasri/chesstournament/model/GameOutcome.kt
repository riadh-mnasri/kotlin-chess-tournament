package io.github.riadhmnasri.chesstournament.model

/** The result of a finished [Game], from a neutral, board-side point of view. */
enum class GameOutcome {
    WHITE_WINS,
    BLACK_WINS,
    DRAW,
}
